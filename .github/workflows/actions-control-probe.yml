name: Public Actions Control Probe

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/actions-control-probe.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Prove hosted runner execution
        shell: bash
        run: |
          set -euo pipefail
          echo "PUBLIC_HOSTED_ACTIONS_OK"
          uname -a
